{
  "name": "mos-card-addons",
  "version": "0.3.0",
  "description": "Home Assistant Lovelace cards for the ha-mos NAS integration: a per-kind title card, a per-server summary card, and a single-device detail card, distributed together as one HACS plugin.",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "deploy": "bash scripts/deploy.sh"
  },
  "license": "MIT",
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "custom-card-helpers": "^1.9.0",
    "home-assistant-js-websocket": "^9.4.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
