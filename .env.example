# Copy to .env (gitignored) and fill in your Home Assistant details.
# Used by scripts/deploy-all.sh (or `npm run deploy --workspace=packages/mos-card-addons`
# directly, from within that package directory) to build and scp the card
# bundle onto your HA instance.
HA_HOST=user@homeassistant.local
HA_CONFIG_PATH=/config
